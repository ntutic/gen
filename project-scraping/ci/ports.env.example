# Pinned by ci/005_deploy.sh on first deploy. Autoselected from the 40xx
# space by scanning ~/code/*/ci for ports already taken; edit or empty to
# reselect. This file is local state and stays out of git.
WEB_PORT=
ADMIN_PORT=
